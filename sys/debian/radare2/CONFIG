PACKAGE=radare2
SECTION=user/editors
PRIORITY=optional
MAINTAINER=pancake <pancake@nopcode.org>

include ../../../config-user.mk
ARCH=$(shell uname -m)
ifeq ($(ARCH),x86_64)
ARCH=amd64
endif
#VERSION=0.10.0
#ARCH=iphoneos-arm
