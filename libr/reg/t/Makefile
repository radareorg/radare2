include ../../config.mk

OBJ=test.o
BIN=regtest${EXT_EXE}
BINDEPS=r_reg r_util

#all: reg${EXT_EXE}

#regtest${EXT_EXE}:
#	${CC} -L../../util -I ../../include -Wl,-R.. -L.. test.c -lr_util -lr_reg -o regtest${EXT_EXE}

#clean:
#	rm -f test a.out

include ../../rules.mk
