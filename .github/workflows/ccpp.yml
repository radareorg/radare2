name: Radare2 CI

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node_version: ['12']
        os: [ubuntu-latest, macOS-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: Configure with ACR
      run: ./configure --prefix=${HOME}
    - name: Build
      run: make
    - name: Install and run tests
      run: |
        # Install the radare2
        make install > /dev/null || exit 1
        export PATH=${HOME}/install/bin:${PATH}
        export LD_LIBRARY_PATH=${HOME}/install/lib:${HOME}/install/lib64:${LD_LIBRARY_PATH}
        export PKG_CONFIG_PATH=${HOME}/pkgcfg:${PKG_CONFIG_PATH}
        # Running the new V suite
        cd test && make src/r2r

